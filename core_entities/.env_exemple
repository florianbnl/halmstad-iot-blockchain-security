RPC_URL=http://host.docker.internal:7545 # Ganache address for Docker

CONTRACT_ADDRESS=0x... # Deployed contract address on Ganache

PRIVATE_KEY=0x... # Private key of the manufacturer account

MQTT_BROKER=mqtt-broker # MQTT broker name in docker-compose